server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rubrica
    username: postgres
    password: lorenzo
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show_sql: true
  main:
    web-application-type: servlet
